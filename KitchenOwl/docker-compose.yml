version: "3.8"

services:
  kitchenowl:
    image: tombursch/kitchenowl:latest
    environment:
      - TZ=Europe/London
    networks:
      - traefik_proxy
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.KitchenOwl.rule=Host(`meals.itslit`)"
        - "traefik.http.routers.KitchenOwl.entrypoints=web,websecure"
        - "traefik.http.services.kitchenowl.loadbalancer.server.port=8080"
        - "traefik.http.routers.KitchenOwl.tls=true"
        - "traefik.http.routers.kitchenowl.service=kitchenowl"
networks:
  traefik_proxy:
    driver: overlay
    attachable: true
